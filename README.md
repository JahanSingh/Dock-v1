## Manual Installation
- Install nodejs locally
- Clone the repo
- Install deps (npm install)
- Start DB locally
- Change the DB credentials via .env
- npx prisma migrate
- npx prisma generate
- npm run build
- npm start

## Docker Installation
